//sudo ng g i Products
//sudo ng gernerate interface Products
//new Project -> ng new foldernamee --no-standalone
export interface Products {
    id: number,
    name: string,
    price: number
    imge: string
}
